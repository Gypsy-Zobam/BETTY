#include <stdio.h>

/**
 * main - Single digit numbers of base 10
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	for (n = '0'; n <= '9'; n++)
		putchar(0);

	putchar('\n');
	return (0);
}

