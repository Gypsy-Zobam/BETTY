#include <stdio.h>

/**
 * main - Single digit numbers of base 10
 *
 * Return: Always (Success)
 */
int main(void)
{
	int a = 0;

	while (a < 10)
	{
	printf("value of a: %d\n", a);
	a++;
	}
	return (0);
}
