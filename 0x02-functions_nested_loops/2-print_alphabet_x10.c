#include "main.h"

/**
 * print_alphabet_x10 - print alphabet_x10 in lowercase
 *
 * Return: Always 0
 */
void print_alphabet_x10(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	{
		ch = 'a';
		while (ch <= 'z')
		{
			_putchar(ch);
			ch++;
		}
		_putchar('\n');
	}
}
